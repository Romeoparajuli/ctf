# Base URL of this app's own backend API (see server/). Defaults to the
# same-origin "/api" path, which works out of the box with the Vite dev
# proxy configured in vite.config.ts.
# VITE_API_BASE_URL=/api

VITE_APP_ENV=development
